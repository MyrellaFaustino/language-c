#include <stdio.h>
#include <string.h>

int main(){
    char s[N];
    int i;

    printf("Digite um texto: \n");
    gets(s);
    i = strlen(s);
    printf("Tamanho do texto: %d \n", i);

    ler(numeros, 0);

    return 0;
}